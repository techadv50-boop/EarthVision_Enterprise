[project]
name = "earthvision-enterprise"
version = "1.0.0"
description = "EarthVision Enterprise - Commercial Earth Observation Platform"
requires-python = ">=3.12"
authors = [{ name = "EarthVision Technologies" }]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["app/tests"]
