[project]
name = "earthvision-enterprise"
version = "1.0.0"
description = "SAT EYE — Eye In Sky commercial Earth observation platform"
requires-python = ">=3.12"
authors = [{ name = "SAT EYE" }]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["app/tests"]
