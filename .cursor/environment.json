{
  "name": "EarthVision Enterprise",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "backend",
      "command": "cd backend && ./.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000 --app-dir .",
      "description": "FastAPI backend API on http://localhost:8000 (interactive docs at /api/docs). Uses a local SQLite database by default."
    },
    {
      "name": "frontend",
      "command": "cd frontend && npm run dev",
      "description": "Vite + React dev server on http://localhost:5173. Proxies /api requests to the backend on port 8000."
    }
  ],
  "ports": [
    { "name": "backend", "port": 8000 },
    { "name": "frontend", "port": 5173 }
  ]
}
